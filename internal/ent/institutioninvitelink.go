// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	entinstitution "github.com/np-inprove/server/internal/ent/institution"
	"github.com/np-inprove/server/internal/ent/institutioninvitelink"
	"github.com/np-inprove/server/internal/entity/institution"
)

// InstitutionInviteLink is the model entity for the InstitutionInviteLink schema.
type InstitutionInviteLink struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Code used in the invite link
	Code string `json:"code,omitempty"`
	// Role granted in the invite
	Role institution.Role `json:"role,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the InstitutionInviteLinkQuery when eager-loading is set.
	Edges               InstitutionInviteLinkEdges `json:"edges"`
	institution_invites *int
	selectValues        sql.SelectValues
}

// InstitutionInviteLinkEdges holds the relations/edges for other nodes in the graph.
type InstitutionInviteLinkEdges struct {
	// Institution holds the value of the institution edge.
	Institution *Institution `json:"institution,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// InstitutionOrErr returns the Institution value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e InstitutionInviteLinkEdges) InstitutionOrErr() (*Institution, error) {
	if e.loadedTypes[0] {
		if e.Institution == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: entinstitution.Label}
		}
		return e.Institution, nil
	}
	return nil, &NotLoadedError{edge: "institution"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InstitutionInviteLink) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case institutioninvitelink.FieldID:
			values[i] = new(sql.NullInt64)
		case institutioninvitelink.FieldCode, institutioninvitelink.FieldRole:
			values[i] = new(sql.NullString)
		case institutioninvitelink.ForeignKeys[0]: // institution_invites
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InstitutionInviteLink fields.
func (iil *InstitutionInviteLink) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case institutioninvitelink.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			iil.ID = int(value.Int64)
		case institutioninvitelink.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				iil.Code = value.String
			}
		case institutioninvitelink.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				iil.Role = institution.Role(value.String)
			}
		case institutioninvitelink.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field institution_invites", value)
			} else if value.Valid {
				iil.institution_invites = new(int)
				*iil.institution_invites = int(value.Int64)
			}
		default:
			iil.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the InstitutionInviteLink.
// This includes values selected through modifiers, order, etc.
func (iil *InstitutionInviteLink) Value(name string) (ent.Value, error) {
	return iil.selectValues.Get(name)
}

// QueryInstitution queries the "institution" edge of the InstitutionInviteLink entity.
func (iil *InstitutionInviteLink) QueryInstitution() *InstitutionQuery {
	return NewInstitutionInviteLinkClient(iil.config).QueryInstitution(iil)
}

// Update returns a builder for updating this InstitutionInviteLink.
// Note that you need to call InstitutionInviteLink.Unwrap() before calling this method if this InstitutionInviteLink
// was returned from a transaction, and the transaction was committed or rolled back.
func (iil *InstitutionInviteLink) Update() *InstitutionInviteLinkUpdateOne {
	return NewInstitutionInviteLinkClient(iil.config).UpdateOne(iil)
}

// Unwrap unwraps the InstitutionInviteLink entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (iil *InstitutionInviteLink) Unwrap() *InstitutionInviteLink {
	_tx, ok := iil.config.driver.(*txDriver)
	if !ok {
		panic("ent: InstitutionInviteLink is not a transactional entity")
	}
	iil.config.driver = _tx.drv
	return iil
}

// String implements the fmt.Stringer.
func (iil *InstitutionInviteLink) String() string {
	var builder strings.Builder
	builder.WriteString("InstitutionInviteLink(")
	builder.WriteString(fmt.Sprintf("id=%v, ", iil.ID))
	builder.WriteString("code=")
	builder.WriteString(iil.Code)
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(fmt.Sprintf("%v", iil.Role))
	builder.WriteByte(')')
	return builder.String()
}

// InstitutionInviteLinks is a parsable slice of InstitutionInviteLink.
type InstitutionInviteLinks []*InstitutionInviteLink
