// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/np-inprove/server/internal/ent/accessory"
	"github.com/np-inprove/server/internal/ent/predicate"
	"github.com/np-inprove/server/internal/ent/redemption"
	"github.com/np-inprove/server/internal/ent/user"
	"github.com/np-inprove/server/internal/ent/voucher"
)

// RedemptionQuery is the builder for querying Redemption entities.
type RedemptionQuery struct {
	config
	ctx           *QueryContext
	order         []redemption.OrderOption
	inters        []Interceptor
	predicates    []predicate.Redemption
	withVoucher   *VoucherQuery
	withAccessory *AccessoryQuery
	withUser      *UserQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RedemptionQuery builder.
func (rq *RedemptionQuery) Where(ps ...predicate.Redemption) *RedemptionQuery {
	rq.predicates = append(rq.predicates, ps...)
	return rq
}

// Limit the number of records to be returned by this query.
func (rq *RedemptionQuery) Limit(limit int) *RedemptionQuery {
	rq.ctx.Limit = &limit
	return rq
}

// Offset to start from.
func (rq *RedemptionQuery) Offset(offset int) *RedemptionQuery {
	rq.ctx.Offset = &offset
	return rq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rq *RedemptionQuery) Unique(unique bool) *RedemptionQuery {
	rq.ctx.Unique = &unique
	return rq
}

// Order specifies how the records should be ordered.
func (rq *RedemptionQuery) Order(o ...redemption.OrderOption) *RedemptionQuery {
	rq.order = append(rq.order, o...)
	return rq
}

// QueryVoucher chains the current query on the "voucher" edge.
func (rq *RedemptionQuery) QueryVoucher() *VoucherQuery {
	query := (&VoucherClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(redemption.Table, redemption.FieldID, selector),
			sqlgraph.To(voucher.Table, voucher.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, redemption.VoucherTable, redemption.VoucherColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAccessory chains the current query on the "accessory" edge.
func (rq *RedemptionQuery) QueryAccessory() *AccessoryQuery {
	query := (&AccessoryClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(redemption.Table, redemption.FieldID, selector),
			sqlgraph.To(accessory.Table, accessory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, redemption.AccessoryTable, redemption.AccessoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (rq *RedemptionQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(redemption.Table, redemption.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, redemption.UserTable, redemption.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Redemption entity from the query.
// Returns a *NotFoundError when no Redemption was found.
func (rq *RedemptionQuery) First(ctx context.Context) (*Redemption, error) {
	nodes, err := rq.Limit(1).All(setContextOp(ctx, rq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{redemption.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rq *RedemptionQuery) FirstX(ctx context.Context) *Redemption {
	node, err := rq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Redemption ID from the query.
// Returns a *NotFoundError when no Redemption ID was found.
func (rq *RedemptionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rq.Limit(1).IDs(setContextOp(ctx, rq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{redemption.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rq *RedemptionQuery) FirstIDX(ctx context.Context) int {
	id, err := rq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Redemption entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Redemption entity is found.
// Returns a *NotFoundError when no Redemption entities are found.
func (rq *RedemptionQuery) Only(ctx context.Context) (*Redemption, error) {
	nodes, err := rq.Limit(2).All(setContextOp(ctx, rq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{redemption.Label}
	default:
		return nil, &NotSingularError{redemption.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rq *RedemptionQuery) OnlyX(ctx context.Context) *Redemption {
	node, err := rq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Redemption ID in the query.
// Returns a *NotSingularError when more than one Redemption ID is found.
// Returns a *NotFoundError when no entities are found.
func (rq *RedemptionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rq.Limit(2).IDs(setContextOp(ctx, rq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{redemption.Label}
	default:
		err = &NotSingularError{redemption.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rq *RedemptionQuery) OnlyIDX(ctx context.Context) int {
	id, err := rq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Redemptions.
func (rq *RedemptionQuery) All(ctx context.Context) ([]*Redemption, error) {
	ctx = setContextOp(ctx, rq.ctx, "All")
	if err := rq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Redemption, *RedemptionQuery]()
	return withInterceptors[[]*Redemption](ctx, rq, qr, rq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rq *RedemptionQuery) AllX(ctx context.Context) []*Redemption {
	nodes, err := rq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Redemption IDs.
func (rq *RedemptionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rq.ctx.Unique == nil && rq.path != nil {
		rq.Unique(true)
	}
	ctx = setContextOp(ctx, rq.ctx, "IDs")
	if err = rq.Select(redemption.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rq *RedemptionQuery) IDsX(ctx context.Context) []int {
	ids, err := rq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rq *RedemptionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rq.ctx, "Count")
	if err := rq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rq, querierCount[*RedemptionQuery](), rq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rq *RedemptionQuery) CountX(ctx context.Context) int {
	count, err := rq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rq *RedemptionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rq.ctx, "Exist")
	switch _, err := rq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rq *RedemptionQuery) ExistX(ctx context.Context) bool {
	exist, err := rq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RedemptionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rq *RedemptionQuery) Clone() *RedemptionQuery {
	if rq == nil {
		return nil
	}
	return &RedemptionQuery{
		config:        rq.config,
		ctx:           rq.ctx.Clone(),
		order:         append([]redemption.OrderOption{}, rq.order...),
		inters:        append([]Interceptor{}, rq.inters...),
		predicates:    append([]predicate.Redemption{}, rq.predicates...),
		withVoucher:   rq.withVoucher.Clone(),
		withAccessory: rq.withAccessory.Clone(),
		withUser:      rq.withUser.Clone(),
		// clone intermediate query.
		sql:  rq.sql.Clone(),
		path: rq.path,
	}
}

// WithVoucher tells the query-builder to eager-load the nodes that are connected to
// the "voucher" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *RedemptionQuery) WithVoucher(opts ...func(*VoucherQuery)) *RedemptionQuery {
	query := (&VoucherClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withVoucher = query
	return rq
}

// WithAccessory tells the query-builder to eager-load the nodes that are connected to
// the "accessory" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *RedemptionQuery) WithAccessory(opts ...func(*AccessoryQuery)) *RedemptionQuery {
	query := (&AccessoryClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withAccessory = query
	return rq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *RedemptionQuery) WithUser(opts ...func(*UserQuery)) *RedemptionQuery {
	query := (&UserClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withUser = query
	return rq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RedeemedAt time.Time `json:"redeemed_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Redemption.Query().
//		GroupBy(redemption.FieldRedeemedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rq *RedemptionQuery) GroupBy(field string, fields ...string) *RedemptionGroupBy {
	rq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RedemptionGroupBy{build: rq}
	grbuild.flds = &rq.ctx.Fields
	grbuild.label = redemption.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RedeemedAt time.Time `json:"redeemed_at,omitempty"`
//	}
//
//	client.Redemption.Query().
//		Select(redemption.FieldRedeemedAt).
//		Scan(ctx, &v)
func (rq *RedemptionQuery) Select(fields ...string) *RedemptionSelect {
	rq.ctx.Fields = append(rq.ctx.Fields, fields...)
	sbuild := &RedemptionSelect{RedemptionQuery: rq}
	sbuild.label = redemption.Label
	sbuild.flds, sbuild.scan = &rq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RedemptionSelect configured with the given aggregations.
func (rq *RedemptionQuery) Aggregate(fns ...AggregateFunc) *RedemptionSelect {
	return rq.Select().Aggregate(fns...)
}

func (rq *RedemptionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rq); err != nil {
				return err
			}
		}
	}
	for _, f := range rq.ctx.Fields {
		if !redemption.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rq.path != nil {
		prev, err := rq.path(ctx)
		if err != nil {
			return err
		}
		rq.sql = prev
	}
	return nil
}

func (rq *RedemptionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Redemption, error) {
	var (
		nodes       = []*Redemption{}
		withFKs     = rq.withFKs
		_spec       = rq.querySpec()
		loadedTypes = [3]bool{
			rq.withVoucher != nil,
			rq.withAccessory != nil,
			rq.withUser != nil,
		}
	)
	if rq.withVoucher != nil || rq.withAccessory != nil || rq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, redemption.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Redemption).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Redemption{config: rq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rq.withVoucher; query != nil {
		if err := rq.loadVoucher(ctx, query, nodes, nil,
			func(n *Redemption, e *Voucher) { n.Edges.Voucher = e }); err != nil {
			return nil, err
		}
	}
	if query := rq.withAccessory; query != nil {
		if err := rq.loadAccessory(ctx, query, nodes, nil,
			func(n *Redemption, e *Accessory) { n.Edges.Accessory = e }); err != nil {
			return nil, err
		}
	}
	if query := rq.withUser; query != nil {
		if err := rq.loadUser(ctx, query, nodes, nil,
			func(n *Redemption, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rq *RedemptionQuery) loadVoucher(ctx context.Context, query *VoucherQuery, nodes []*Redemption, init func(*Redemption), assign func(*Redemption, *Voucher)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Redemption)
	for i := range nodes {
		if nodes[i].voucher_redemptions == nil {
			continue
		}
		fk := *nodes[i].voucher_redemptions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(voucher.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "voucher_redemptions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rq *RedemptionQuery) loadAccessory(ctx context.Context, query *AccessoryQuery, nodes []*Redemption, init func(*Redemption), assign func(*Redemption, *Accessory)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Redemption)
	for i := range nodes {
		if nodes[i].accessory_redemptions == nil {
			continue
		}
		fk := *nodes[i].accessory_redemptions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(accessory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "accessory_redemptions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rq *RedemptionQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*Redemption, init func(*Redemption), assign func(*Redemption, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Redemption)
	for i := range nodes {
		if nodes[i].redemption_user == nil {
			continue
		}
		fk := *nodes[i].redemption_user
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "redemption_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rq *RedemptionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rq.querySpec()
	_spec.Node.Columns = rq.ctx.Fields
	if len(rq.ctx.Fields) > 0 {
		_spec.Unique = rq.ctx.Unique != nil && *rq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rq.driver, _spec)
}

func (rq *RedemptionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(redemption.Table, redemption.Columns, sqlgraph.NewFieldSpec(redemption.FieldID, field.TypeInt))
	_spec.From = rq.sql
	if unique := rq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rq.path != nil {
		_spec.Unique = true
	}
	if fields := rq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, redemption.FieldID)
		for i := range fields {
			if fields[i] != redemption.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rq *RedemptionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rq.driver.Dialect())
	t1 := builder.Table(redemption.Table)
	columns := rq.ctx.Fields
	if len(columns) == 0 {
		columns = redemption.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rq.sql != nil {
		selector = rq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rq.ctx.Unique != nil && *rq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rq.predicates {
		p(selector)
	}
	for _, p := range rq.order {
		p(selector)
	}
	if offset := rq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RedemptionGroupBy is the group-by builder for Redemption entities.
type RedemptionGroupBy struct {
	selector
	build *RedemptionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rgb *RedemptionGroupBy) Aggregate(fns ...AggregateFunc) *RedemptionGroupBy {
	rgb.fns = append(rgb.fns, fns...)
	return rgb
}

// Scan applies the selector query and scans the result into the given value.
func (rgb *RedemptionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rgb.build.ctx, "GroupBy")
	if err := rgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RedemptionQuery, *RedemptionGroupBy](ctx, rgb.build, rgb, rgb.build.inters, v)
}

func (rgb *RedemptionGroupBy) sqlScan(ctx context.Context, root *RedemptionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rgb.fns))
	for _, fn := range rgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rgb.flds)+len(rgb.fns))
		for _, f := range *rgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RedemptionSelect is the builder for selecting fields of Redemption entities.
type RedemptionSelect struct {
	*RedemptionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rs *RedemptionSelect) Aggregate(fns ...AggregateFunc) *RedemptionSelect {
	rs.fns = append(rs.fns, fns...)
	return rs
}

// Scan applies the selector query and scans the result into the given value.
func (rs *RedemptionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rs.ctx, "Select")
	if err := rs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RedemptionQuery, *RedemptionSelect](ctx, rs.RedemptionQuery, rs, rs.inters, v)
}

func (rs *RedemptionSelect) sqlScan(ctx context.Context, root *RedemptionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rs.fns))
	for _, fn := range rs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
