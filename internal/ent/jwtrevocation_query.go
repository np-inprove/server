// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/np-inprove/server/internal/ent/jwtrevocation"
	"github.com/np-inprove/server/internal/ent/predicate"
)

// JWTRevocationQuery is the builder for querying JWTRevocation entities.
type JWTRevocationQuery struct {
	config
	ctx        *QueryContext
	order      []jwtrevocation.OrderOption
	inters     []Interceptor
	predicates []predicate.JWTRevocation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JWTRevocationQuery builder.
func (jrq *JWTRevocationQuery) Where(ps ...predicate.JWTRevocation) *JWTRevocationQuery {
	jrq.predicates = append(jrq.predicates, ps...)
	return jrq
}

// Limit the number of records to be returned by this query.
func (jrq *JWTRevocationQuery) Limit(limit int) *JWTRevocationQuery {
	jrq.ctx.Limit = &limit
	return jrq
}

// Offset to start from.
func (jrq *JWTRevocationQuery) Offset(offset int) *JWTRevocationQuery {
	jrq.ctx.Offset = &offset
	return jrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jrq *JWTRevocationQuery) Unique(unique bool) *JWTRevocationQuery {
	jrq.ctx.Unique = &unique
	return jrq
}

// Order specifies how the records should be ordered.
func (jrq *JWTRevocationQuery) Order(o ...jwtrevocation.OrderOption) *JWTRevocationQuery {
	jrq.order = append(jrq.order, o...)
	return jrq
}

// First returns the first JWTRevocation entity from the query.
// Returns a *NotFoundError when no JWTRevocation was found.
func (jrq *JWTRevocationQuery) First(ctx context.Context) (*JWTRevocation, error) {
	nodes, err := jrq.Limit(1).All(setContextOp(ctx, jrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jwtrevocation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jrq *JWTRevocationQuery) FirstX(ctx context.Context) *JWTRevocation {
	node, err := jrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JWTRevocation ID from the query.
// Returns a *NotFoundError when no JWTRevocation ID was found.
func (jrq *JWTRevocationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jrq.Limit(1).IDs(setContextOp(ctx, jrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jwtrevocation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jrq *JWTRevocationQuery) FirstIDX(ctx context.Context) int {
	id, err := jrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JWTRevocation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JWTRevocation entity is found.
// Returns a *NotFoundError when no JWTRevocation entities are found.
func (jrq *JWTRevocationQuery) Only(ctx context.Context) (*JWTRevocation, error) {
	nodes, err := jrq.Limit(2).All(setContextOp(ctx, jrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jwtrevocation.Label}
	default:
		return nil, &NotSingularError{jwtrevocation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jrq *JWTRevocationQuery) OnlyX(ctx context.Context) *JWTRevocation {
	node, err := jrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JWTRevocation ID in the query.
// Returns a *NotSingularError when more than one JWTRevocation ID is found.
// Returns a *NotFoundError when no entities are found.
func (jrq *JWTRevocationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jrq.Limit(2).IDs(setContextOp(ctx, jrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jwtrevocation.Label}
	default:
		err = &NotSingularError{jwtrevocation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jrq *JWTRevocationQuery) OnlyIDX(ctx context.Context) int {
	id, err := jrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JWTRevocations.
func (jrq *JWTRevocationQuery) All(ctx context.Context) ([]*JWTRevocation, error) {
	ctx = setContextOp(ctx, jrq.ctx, "All")
	if err := jrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JWTRevocation, *JWTRevocationQuery]()
	return withInterceptors[[]*JWTRevocation](ctx, jrq, qr, jrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jrq *JWTRevocationQuery) AllX(ctx context.Context) []*JWTRevocation {
	nodes, err := jrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JWTRevocation IDs.
func (jrq *JWTRevocationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if jrq.ctx.Unique == nil && jrq.path != nil {
		jrq.Unique(true)
	}
	ctx = setContextOp(ctx, jrq.ctx, "IDs")
	if err = jrq.Select(jwtrevocation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jrq *JWTRevocationQuery) IDsX(ctx context.Context) []int {
	ids, err := jrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jrq *JWTRevocationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jrq.ctx, "Count")
	if err := jrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jrq, querierCount[*JWTRevocationQuery](), jrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jrq *JWTRevocationQuery) CountX(ctx context.Context) int {
	count, err := jrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jrq *JWTRevocationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jrq.ctx, "Exist")
	switch _, err := jrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jrq *JWTRevocationQuery) ExistX(ctx context.Context) bool {
	exist, err := jrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JWTRevocationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jrq *JWTRevocationQuery) Clone() *JWTRevocationQuery {
	if jrq == nil {
		return nil
	}
	return &JWTRevocationQuery{
		config:     jrq.config,
		ctx:        jrq.ctx.Clone(),
		order:      append([]jwtrevocation.OrderOption{}, jrq.order...),
		inters:     append([]Interceptor{}, jrq.inters...),
		predicates: append([]predicate.JWTRevocation{}, jrq.predicates...),
		// clone intermediate query.
		sql:  jrq.sql.Clone(),
		path: jrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Jti string `json:"jti,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JWTRevocation.Query().
//		GroupBy(jwtrevocation.FieldJti).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jrq *JWTRevocationQuery) GroupBy(field string, fields ...string) *JWTRevocationGroupBy {
	jrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JWTRevocationGroupBy{build: jrq}
	grbuild.flds = &jrq.ctx.Fields
	grbuild.label = jwtrevocation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Jti string `json:"jti,omitempty"`
//	}
//
//	client.JWTRevocation.Query().
//		Select(jwtrevocation.FieldJti).
//		Scan(ctx, &v)
func (jrq *JWTRevocationQuery) Select(fields ...string) *JWTRevocationSelect {
	jrq.ctx.Fields = append(jrq.ctx.Fields, fields...)
	sbuild := &JWTRevocationSelect{JWTRevocationQuery: jrq}
	sbuild.label = jwtrevocation.Label
	sbuild.flds, sbuild.scan = &jrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JWTRevocationSelect configured with the given aggregations.
func (jrq *JWTRevocationQuery) Aggregate(fns ...AggregateFunc) *JWTRevocationSelect {
	return jrq.Select().Aggregate(fns...)
}

func (jrq *JWTRevocationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jrq); err != nil {
				return err
			}
		}
	}
	for _, f := range jrq.ctx.Fields {
		if !jwtrevocation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jrq.path != nil {
		prev, err := jrq.path(ctx)
		if err != nil {
			return err
		}
		jrq.sql = prev
	}
	return nil
}

func (jrq *JWTRevocationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JWTRevocation, error) {
	var (
		nodes = []*JWTRevocation{}
		_spec = jrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JWTRevocation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JWTRevocation{config: jrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (jrq *JWTRevocationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jrq.querySpec()
	_spec.Node.Columns = jrq.ctx.Fields
	if len(jrq.ctx.Fields) > 0 {
		_spec.Unique = jrq.ctx.Unique != nil && *jrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jrq.driver, _spec)
}

func (jrq *JWTRevocationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jwtrevocation.Table, jwtrevocation.Columns, sqlgraph.NewFieldSpec(jwtrevocation.FieldID, field.TypeInt))
	_spec.From = jrq.sql
	if unique := jrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jrq.path != nil {
		_spec.Unique = true
	}
	if fields := jrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jwtrevocation.FieldID)
		for i := range fields {
			if fields[i] != jwtrevocation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jrq *JWTRevocationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jrq.driver.Dialect())
	t1 := builder.Table(jwtrevocation.Table)
	columns := jrq.ctx.Fields
	if len(columns) == 0 {
		columns = jwtrevocation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jrq.sql != nil {
		selector = jrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jrq.ctx.Unique != nil && *jrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range jrq.predicates {
		p(selector)
	}
	for _, p := range jrq.order {
		p(selector)
	}
	if offset := jrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JWTRevocationGroupBy is the group-by builder for JWTRevocation entities.
type JWTRevocationGroupBy struct {
	selector
	build *JWTRevocationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jrgb *JWTRevocationGroupBy) Aggregate(fns ...AggregateFunc) *JWTRevocationGroupBy {
	jrgb.fns = append(jrgb.fns, fns...)
	return jrgb
}

// Scan applies the selector query and scans the result into the given value.
func (jrgb *JWTRevocationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jrgb.build.ctx, "GroupBy")
	if err := jrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JWTRevocationQuery, *JWTRevocationGroupBy](ctx, jrgb.build, jrgb, jrgb.build.inters, v)
}

func (jrgb *JWTRevocationGroupBy) sqlScan(ctx context.Context, root *JWTRevocationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jrgb.fns))
	for _, fn := range jrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jrgb.flds)+len(jrgb.fns))
		for _, f := range *jrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JWTRevocationSelect is the builder for selecting fields of JWTRevocation entities.
type JWTRevocationSelect struct {
	*JWTRevocationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jrs *JWTRevocationSelect) Aggregate(fns ...AggregateFunc) *JWTRevocationSelect {
	jrs.fns = append(jrs.fns, fns...)
	return jrs
}

// Scan applies the selector query and scans the result into the given value.
func (jrs *JWTRevocationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jrs.ctx, "Select")
	if err := jrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JWTRevocationQuery, *JWTRevocationSelect](ctx, jrs.JWTRevocationQuery, jrs, jrs.inters, v)
}

func (jrs *JWTRevocationSelect) sqlScan(ctx context.Context, root *JWTRevocationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jrs.fns))
	for _, fn := range jrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
